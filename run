#!/usr/local/bin/clj

(require '[stillsuit-sample.main :as main]
         '[clojure.tools.logging :as log])

(try
  (apply main/-main *command-line-args*)
  (catch Exception e
    (log/error e (.getCause e))
    (System/exit 1)))
